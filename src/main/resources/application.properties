# serivce name
spring.application.name=bipro-ws-config-server

# port
server.port=8780

# eureka server url
eureka.client.service-url.default-zone=http://localhost:8761/eureka

# security
management.security.enabled=false

# logging
logging.level.org.springframework.ws=TRACE